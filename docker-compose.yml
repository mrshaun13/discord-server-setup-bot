version: '3.8'

services:
  discord-setup-bot:
    build: .
    container_name: discord-ai-setup-bot
    restart: "no"  # Don't restart - this is a one-time setup
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    environment:
      - PYTHONUNBUFFERED=1
